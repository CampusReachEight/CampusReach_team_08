package com.android.sample.ui.profile.publicProfile

/** Shared constants and defaults used by PublicProfile UI and helpers. */
object PublicProfileDefaults {
  const val DATE_FORMAT_PATTERN = "dd/MM/yyyy"
  const val DEFAULT_NAME = "Unknown"
  const val DEFAULT_SECTION = "None"
  const val EMPTY = ""
  const val DEFAULT_PROFILE_ID = ""
}
