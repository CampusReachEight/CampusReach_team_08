package com.android.sample.ui.profile.publicProfile

/**
 * Error and fallback messages used by PublicProfile feature. Move to resources when localization is
 * required.
 */
object PublicProfileErrors {
  const val EMPTY_PROFILE_ID = "Profile id must not be empty"
  const val PROFILE_NOT_FOUND = "Profile not found"
  const val FAILED_TO_LOAD = "Failed to load profile"
}
